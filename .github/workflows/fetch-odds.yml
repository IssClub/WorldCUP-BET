name: Fetch and lock odds

on:
  schedule:
    # כל 4 שעות בחודשי הטורניר (יוני–יולי 2026)
    # עם commenceTimeTo=24h מחזיר 2-5 משחקים בלבד ← ~3-10 קרדיטים לקריאה
    - cron: '0 0,4,8,12,16,20 * 6,7 *'
    # פעם ביום מחוץ לטורניר (יחסים ראשוניים, לא רלוונטי לפני יוני)
    - cron: '0 12 * 1-5,8-12 *'

  workflow_dispatch:  # כפתור הפעלה ידנית

jobs:
  fetch-odds:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - name: Fetch and lock odds
        run: node scripts/fetch-odds.mjs
        env:
          SUPABASE_URL:         ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          ODDS_API_KEY:         ${{ secrets.VITE_ODDS_API_KEY }}
