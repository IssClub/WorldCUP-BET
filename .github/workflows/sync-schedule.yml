name: Sync League Schedule

on:
  schedule:
    - cron: '0 2 * * 2'    # כל שלישי 02:00 UTC = 05:00 ישראל
    - cron: '0 2 * * 3'    # כל רביעי 02:00 UTC = 05:00 ישראל
  workflow_dispatch:         # כפתור הרצה ידנית מ-GitHub

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install @supabase/supabase-js

      - name: Run schedule sync
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: node scripts/seed-league-schedule.mjs
